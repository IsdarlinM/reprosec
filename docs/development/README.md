# Development

Install sibling SRIC Core, then ReproSec dev dependencies. Run `pytest`, `ruff check src tests`, `python -m compileall -q src`; under `web/`, run `npm ci` and `npm run build`. Importers and replay changes require adversarial/security tests.
