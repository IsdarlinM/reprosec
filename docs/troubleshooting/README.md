# Troubleshooting

Run `reprosec doctor`. `reprosec verify` exits non-zero on manifest/signature/archive failures. Replay errors explicitly identify Policy or Scope denial. Non-loopback Web binding is intentionally disabled until authenticated TLS mode is implemented.
