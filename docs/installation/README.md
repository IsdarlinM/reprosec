# Installation

ReproSec v0.2 depends on SRIC Core v0.2. In this source workspace, the installers detect the sibling `sric-core` project and install it first.

Linux: `./scripts/install-linux.sh`; Windows: `scripts\install-windows.cmd`. Verify with `reprosec doctor`.
