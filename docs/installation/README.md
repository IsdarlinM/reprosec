# Installation

ReproSec v0.4 depends on SRIC Core v0.4. In a source workspace, installers prefer an explicitly configured or sibling `sric-core` source before resolving a packaged dependency.

Linux: `./scripts/install-linux.sh`; Windows: `scripts\install-windows.cmd`. Verify with `reprosec doctor`.
