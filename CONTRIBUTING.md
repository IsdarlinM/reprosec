# Contributing

All public features require code, input validation, error handling, tests, docs and security review. Importers must treat every byte as hostile. Replay changes must add tests proving Scope/Policy gates cannot be bypassed.
