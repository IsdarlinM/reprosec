# Roadmap

## Current — 0.5.0 release candidate

Implemented in the 0.5 train:
- SRIC 0.5 integration and evidence-native RCAP research context for Sentinel Cases.
- Scope snapshots, policy decisions, validation recipes, tool provenance and counter-evidence references.
- Destructive policy-decision approval guards and deterministic research-context digests.
- Formal RCAP 0.3 specification and corrected current schema pointer.
- CLI/API surfaces for research-context validation/inspection.
- Coordinated release-evidence gate v2 and local internal-wheel dependency support.

## Next hardening
- Explicit opt-in TLS interception adapter with strict certificate lifecycle/authorization controls; never silent MITM.
- Richer replay cohorts and variance normalization for dynamic applications.
- Long-running import/replay jobs over the SRIC Job Engine with resumability and SSE progress.
- Larger public RCAP conformance corpus and third-party implementation compatibility testing.
- Browser E2E, parser property fuzzing and signed release attestations.

## 1.0
Stable RCAP schema/governance, migration compatibility, signed releases, external implementations/conformance and audited replay security model.
